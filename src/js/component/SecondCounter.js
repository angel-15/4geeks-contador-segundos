import React from "react";

/* eslint-disable react/prop-types */

export function SecondCounter({ cero1, cero2, cero3, cero4, cero5, cero6 }) {
	return (
		<div>
			{`${cero1} - ${cero2} - ${cero3} - ${cero4} - ${cero5} - ${cero6}`}
		</div>
	);
}
